import React from 'react';

const ErrorPage = ({error}) => {
    return (
        <div>
            < div style={{background: 'red', zIndex: '1031' }}>Submission error! </div>
        </div>
    );
};

export default ErrorPage;